# Railway deployment configuration for the Cognee MCP service

[build]
dockerfilePath = "Dockerfile.mcp"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 240
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
numReplicas = 1
